<phone:PhoneApplicationPage 
    x:Class="ComicBrowser.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:comic="clr-namespace:ComicBrowser"

    mc:Ignorable="d" d:DesignWidth="811" d:DesignHeight="768" 
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="PortraitOrLandscape"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <StackPanel>
            <toolkit:PerformanceProgressBar IsIndeterminate="{Binding ComicLoading}" Foreground="Green"/>

            <!--Pivot Control-->
            <controls:Pivot x:Name="TopPivot" Title="Daily Comics" SelectionChanged="Pivot_SelectionChanged">
                <!--Pivot item for Fingerpori -->
                <controls:PivotItem Header="Fingerpori">
                    <comic:ComicView HorizontalAlignment="Center" VerticalAlignment="Center" DataContext="{Binding}" />
                </controls:PivotItem>

                <!--Pivot item for Viivi ja Wagner-->
                <controls:PivotItem Header="Viivi &amp; Wagner">
                    <comic:ComicView HorizontalAlignment="Center" VerticalAlignment="Center" DataContext="{Binding}" />
                </controls:PivotItem>

                <!--Pivot item for Sinfest-->
                <controls:PivotItem Header="Sinfest">
                    <comic:ComicView HorizontalAlignment="Center" VerticalAlignment="Center" DataContext="{Binding}" />
                </controls:PivotItem>

                <!--Pivot item for Dilbert-->
                <controls:PivotItem Header="Dilbert">
                    <comic:ComicView HorizontalAlignment="Center" VerticalAlignment="Center" DataContext="{Binding}" />
                </controls:PivotItem>
                
                <!--Pivot item for User Friendly-->
                <controls:PivotItem Header="UserFriendly">
                    <comic:ComicView HorizontalAlignment="Center" VerticalAlignment="Center" DataContext="{Binding}" />
                </controls:PivotItem>
            </controls:Pivot>
        </StackPanel>
    </Grid>

    <!--Sample code showing usage of ApplicationBar-->
    <!--<phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button1.png" Text="Button 1"/>
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button2.png" Text="Button 2"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>-->

</phone:PhoneApplicationPage>